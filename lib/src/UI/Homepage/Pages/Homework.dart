import 'package:flutter/material.dart';

// TODO
class HomepageHomework extends StatelessWidget {
  const HomepageHomework({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.limeAccent,
    );
  }
}
