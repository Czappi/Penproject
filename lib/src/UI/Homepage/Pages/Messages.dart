import 'package:flutter/material.dart';

// TODO
class HomepageMessages extends StatelessWidget {
  const HomepageMessages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.indigo);
  }
}
