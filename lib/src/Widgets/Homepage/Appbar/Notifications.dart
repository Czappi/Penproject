import 'package:flutter/material.dart';

class HomepageAppbarNotifications extends StatelessWidget {
  const HomepageAppbarNotifications({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return FlatButton(onPressed: null, child: null);
  }
}
